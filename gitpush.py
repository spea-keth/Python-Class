print("Introduction to GitHub")
    #
    # git init - initializes/creates a new git repo on the local machine
    # root - staging area - GitHub
    # git commit - prepare files & folders to push online (must be w/ message)
    # add . - add everything
    # add pythonfile - add a single file
    # git commit -m 'Message Here'

    """
    git init
        to working directory
    git add
        to staging area
    git commit -m 'message'
        to path on the way to (online) repo
    git push
    """